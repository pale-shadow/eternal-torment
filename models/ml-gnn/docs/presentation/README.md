# Presentation

```sh
make presentation
sudo apt install libgirepository1.0-dev
python3 -m pip install -r requirements.txt
```

Now you can present using the `pympress` progam.

## Formatting

```sh
chktex gnn-pres.tex
```

